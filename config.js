const config = {
  settings: {
    subfolders: true,
    routes: {
      HOMEPAGE: '/',
      ANALYTICS: '/analytics',
      DETECTIONS: '/detect',
      EXPLORER: '/explore',
      LOGIN: '/login',
      REGISTER: '/register',
      SITE: '/site',
      CAMERA: '/camera',
      //MASK_EDIT: '/mask/edit/:id',
      //MASK_UPDATE: '/mask/update/:id',
    }
  }
}

export default config